To build this project you will need 

   1. nlohmann json.
   2. libfmt.
   3. boost > 1.70.

On debian systems 1. and 2. can be installed from packages

   $ sudo apt-get install nlohmann-json3-dev
   $ sudo apt-get install libfmt3-dev

